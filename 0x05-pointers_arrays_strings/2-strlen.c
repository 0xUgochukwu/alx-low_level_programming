#include <string.h>
#include "main.h"


/**
 * _strlen - get the length of a string
 * @s: pointer to the string to be used
 * Return: length of string
 */


int _strlen(char *s)
{
	return (strlen(s));
}
